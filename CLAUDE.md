# 프로젝트 컨텍스트 (Claude Code용 핸드오프 문서)

이 문서는 VSCode의 Claude Code 세션이 이 저장소를 처음 열었을 때 바로 맥락을 잡을 수 있도록,
지금까지 팀이 논의하고 결정한 내용을 정리한 것입니다. 새 작업을 시작하기 전에 이 문서를 먼저 읽고
아래 "지금 결정이 필요한 것" 섹션부터 팀원과 함께 확인해주세요.

## 1. 문제의식

메일이나 공지 등 공적인 글을 쓸 때 포멀한 어휘가 잘 떠오르지 않아 AI에게 물어봐도, 결과가 내가
평소 쓰는 어휘·어조와 달라 오히려 쓰기 불편했다는 경험에서 출발했다. 문서를 AI로 요약해도 내
어투와 달라 요약본이 읽기 불편한 문제도 동일한 뿌리를 가진다. 즉 "AI 결과물을 내 말투에 맞게
반영해달라"는 문제의식이 두 아이디어(공적 어휘 추천, 문서 개인화 요약)를 관통한다.

## 2. 기존 시도와 그 한계

처음에는 개인 블로그 글 등을 학습 데이터로 삼아 LoRA로 소형 언어 모델을 직접 파인튜닝하는 방향으로
실습을 진행했다. 결과:

- 데이터 500개 규모로는 특유의 말투를 안정적으로 학습시키기 어려웠음
- 파인튜닝 후 모델의 전반적 언어 능력이 퇴화하는 문제(과적합/재앙적 망각으로 추정) 발생
- 쉽게 구할 수 있는 공개 데이터로는 이미 유사한 프로젝트가 많아 독창성 확보가 어려웠고,
  반대로 팀만의 특수 데이터는 절대량이 부족함
- 학부 스터디 팀 수준의 GPU/데이터 자원으로는 안정적인 파인튜닝 자체가 구조적으로 어려움

## 3. 채택한 방향: 파인튜닝 → 검색 기반(RAG/Few-shot) 전환

모델 가중치를 재학습시키는 대신, 사전학습된 생성 모델(API 기반, 가중치 변경 없음)에
**검색(retrieval) 기능만 추가**해서 개인화를 구현한다.

- 사용자가 과거에 쓴 글을 예시 풀로 저장해둔다.
- 지금 입력(작성 중인 문장 / 요약할 원문)과 유사한 예시를 임베딩 유사도로 검색한다.
- 검색된 예시를 "이 문체를 참고해서 답해줘"라는 형태로 프롬프트에 넣어 생성 모델에 전달한다.
- 모델 가중치는 전혀 바뀌지 않으므로, 예시가 부족해도 언어 능력 자체가 퇴화하는 일은 없다
  (검색이 부실하면 그냥 결과가 "평범한 AI 톤"으로 돌아갈 뿐, 파인튜닝처럼 망가지지 않는다).

이 전환의 핵심 이유:

1. Few-shot 검색은 파인튜닝과 달리 데이터가 적어도(수십 개) 안정적으로 작동한다.
2. 언어능력 퇴화 문제가 구조적으로 사라진다.
3. 지금까지 스터디한 Attention 메커니즘(유사도 점수 → 가중치화 → 가중합)이 검색 로직에
   그대로 응용되므로, 학습 내용과의 연결고리가 명확하다.

## 4. 시스템 구조 (파이프라인)

```
[내가 쓴 글 예시 수집]
        │
        ▼
[임베딩 → 벡터 저장 (예시 풀)]
        │
[입력: 작성 중인 문장 또는 요약할 원문]
        │
        ▼
[임베딩 → 유사 예시 Top-K 검색 (코사인 유사도)]
        │
        ▼
[프롬프트 구성: "이 예시들처럼 써줘" + 작업 지시]
        │
        ▼
[생성 모델 호출 → 문체 반영 결과]
        │
        ▼ (선택)
[사용자 피드백/수정 → 예시 풀에 추가]
```

두 가지 기능을 같은 엔진으로 구현한다.

- **공적 어휘 추천**: 작성 중인 문장을 입력받아, 내 예시 어휘/어조를 참고한 포멀 표현을 추천
- **문서 개인화 요약**: 전문 문서를 입력받아, 내 문체로 요약

## 5. 데이터 스펙

- 형식: 가능하면 `(원문 상황/주제, 내가 쓴 글)` 쌍이 이상적이지만, 없다면 "내가 쓴 글" 원문
  조각만 있어도 동작한다 (문체만 참고).
- 분량: 파인튜닝과 달리 30~100개 정도면 시작 가능. 양보다 상황별 다양성(메일/공지/요약/메모 등)이
  더 중요하다.
- 저장 위치: `data/examples/examples.jsonl` (스키마는 아래 "데이터 포맷" 참고)

## 6. 알려진 실패 모드와 대응

- **예시가 부족/부실한 경우**: 결과가 개인화되지 않고 일반적인 톤으로 나올 뿐, 모델이 망가지지는
  않는다. 유사도 점수가 임계값 이하면 사용자에게 "참고 예시가 부족합니다" 안내를 띄우는 것을 고려.
- **예시가 아예 없을 때의 기본값**: 상황별 예시가 없을 때를 대비해 "전반적인 내 문체를 보여주는
  기본 예시 세트"를 항상 몇 개 함께 넣는 방식(fallback set)을 고려.
- **피드백 루프는 필수 아님**: MVP는 정적 예시 풀만으로 완전히 동작한다. 피드백 축적은 확장
  기능으로 나중에 붙여도 된다.

## 7. 제약 조건

- 박람회 출품·시연이 목표이므로 기존 배포 서비스를 단순 재현하는 수준으로는 부족함
- 학부 스터디 팀 수준의 데이터/컴퓨팅 자원 — 모델을 새로 학습시키는 방향은 지양
- 시연 형태(실시간 라이브 데모 vs 사전 준비된 사례 시연)에 따라 지연시간 제약이 달라짐 → 아직 미정

## 8. 지금 결정이 필요한 것 (Claude Code 세션에서 팀과 먼저 확인)

1. **생성 모델 선택**: Gemini API(무료 티어, 팀이 이미 사용 경험 있음) vs 다른 제공자. 이 스캐폴드는
   Gemini(`google-genai` SDK)를 기본값으로 깔아두었으니, 다른 걸 쓰려면 `src/generate.py`의
   `call_llm()`만 교체하면 된다.
2. **임베딩 모델 선택**: 이 스캐폴드는 Google의 `text-embedding-004`를 기본으로 사용한다
   (별도 설치 없이 이미 쓰고 있는 API 키로 바로 됨). 로컬에서 무료로 돌리고 싶다면
   `sentence-transformers` 계열(예: `paraphrase-multilingual-MiniLM-L12-v2`)로 교체 가능.
3. **MVP 범위**: 공적 어휘 추천과 문서 요약 중 하나를 먼저 완성할지, 처음부터 둘 다 만들지.
4. **평가 방법**: 스타일 반영 정도를 어떻게 측정할지 (사람이 직접 평가 / 임베딩 거리 기반 정량 평가).
5. **시연 형태 확정**: 실시간 데모 여부에 따라 지연시간·모델 크기 제약이 달라짐.

## 9. 지금까지 스터디한 내용과의 연결 (발표 자료에 활용 가능)

- 검색 로직의 "유사도 계산 → Top-K 선택 → 가중합/프롬프트 반영"은 Attention Is All You Need에서
  공부한 어텐션 3단계 구조의 응용이다.
- 임베딩 모델(Sentence-BERT류)은 Transformer Encoder이며, Multi-Head Attention이 내부에서
  문맥을 포착한다.
- "고정 벡터에 압축(파인튜닝) vs 필요할 때마다 동적 조회(검색/어텐션)"라는 설계 선택은, seq2seq에서
  Attention이 등장한 것과 동일한 논리 구조다.
- 왜 500개 데이터로 파인튜닝하면 퇴화가 일어나는지에 대한 이해(과적합, 그래디언트가 소량 데이터에
  과도하게 맞춰짐)는 "왜 이 방식을 선택했는가"에 대한 기술적 근거로 그대로 쓸 수 있다.

## 10. 저장소 구조

```
persona-rag-starter/
├── CLAUDE.md              # 이 문서
├── README.md              # 실행 방법
├── requirements.txt
├── .env.example
├── data/
│   └── examples/
│       └── examples.jsonl # 내 글 예시 (더미 3개 포함, 실제 데이터로 교체 필요)
├── src/
│   ├── config.py
│   ├── embeddings.py       # 임베딩 API 래퍼 + 로컬 캐시
│   ├── store.py            # 예시 로드 + 임베딩 색인 관리
│   ├── retrieve.py         # 코사인 유사도 기반 Top-K 검색
│   ├── generate.py         # 프롬프트 구성 + 생성 모델 호출
│   └── cli.py              # 커맨드라인 진입점
└── scripts/
    └── build_index.py      # 예시 재색인 스크립트
```

## 11. 다음 단계 제안

1. `data/examples/examples.jsonl`을 팀원들의 실제 글로 채운다 (30~50개부터 시작 권장).
2. `python scripts/build_index.py`로 임베딩 색인을 만든다.
3. `python -m src.cli vocab "문장"` 또는 `python -m src.cli summarize 파일경로`로 동작을 확인한다.
4. 검색이 잘 되는지(관련 없는 예시가 뽑히지 않는지) 먼저 점검한 뒤, 프롬프트 문구를 다듬는다.
5. 위 "지금 결정이 필요한 것" 항목들을 팀과 확정하며 MVP 범위를 좁힌다.
